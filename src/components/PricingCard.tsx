export const PricingCard = () => {
  return (
    <div className="w-full md:w-1/3 h-[650px] flex">
      {/* <div
        className={`${plan.background} ${plan.rounding || ''} flex w-full flex-col text-base text-pastel-lilac font-light px-6 md:px-9 pt-8 md:pt-9 pb-10 h-full`}
      >
        <div className="flex items-center gap-2 text-xl text-soft-lavender font-medium whitespace-nowrap text-center tracking-[-0.4px]">
          <h3 className="self-stretch my-auto">{plan.title}</h3>
          <img
            src={plan.icon}
            alt={`${plan.title} icon`}
            className="aspect-[1] object-contain w-5 self-stretch shrink-0 my-auto"
          />
        </div>
        <div className="text-white text-[40px] font-medium mt-3">
          {plan.price}
          {plan.period && (
            <span className="font-light text-2xl">{plan.period}</span>
          )}
        </div>
        <div className="mt-[60px] flex-grow flex flex-col justify-between">
          <ul className="space-y-6 text-left tracking-[-0.32px]">
            {plan.features.map((feature, index) => (
              <li key={index}>{feature}</li>
            ))}
          </ul>
        </div>
      </div> */}
    </div>
  );
};
